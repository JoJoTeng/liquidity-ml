# Pinned to local-machine versions so HPC and laptop produce identical
# numerical results. Bump these together when intentionally upgrading.

# Core numerics — pinning is critical for reproducibility
numpy==1.26.4
pandas==2.2.3
scipy==1.17.1
scikit-learn==1.8.0

# Models — version differences change predictions and SHAP values
xgboost==3.2.0
tensorflow==2.16.2
keras==3.5.0
shap==0.47.1

# Statistics + IO — pinned for consistent regression and parquet behavior
statsmodels==0.14.6
pyarrow==23.0.1

# Plotting + utilities — looser; cosmetic differences only
matplotlib>=3.7
seaborn>=0.13
jinja2>=3.1
pyyaml>=6.0

# Data fetching
wrds>=3.1
pandas-datareader>=0.10

# Misc
arch>=6.0
pytest>=7.4
